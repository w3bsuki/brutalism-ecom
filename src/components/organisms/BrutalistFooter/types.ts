import { HTMLAttributes } from 'react';

/**
 * Props for the BrutalistFooter component
 */
export interface BrutalistFooterProps extends HTMLAttributes<HTMLElement> {
  /**
   * Optional custom classes to apply to the footer
   */
  className?: string;
} 